function get_name(id) {
    if id == 1 {
       "Niitroxen"
    }
}

function oper(a, b, operation) {
    if operation == "+" {
        a + b;
    }

    if operation == "-" {
        a - b;
    }

    if operation == "*" {
        a * b;
    }

    if operation == "/" {
        a / b;
    }
}

let a = 1;

let b = 2;
let res = oper(a, b, "/");

println(res);